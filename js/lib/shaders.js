// AIRE-Player | Built-in extra.json post-processing shaders
// ============================================================
//  Effects (extra.json effects): built-in shaders
// ============================================================
const BUILTIN_SHADERS = {
  "bokeh": "#version 100\nprecision highp float;\nuniform float uScale;\n\nvarying vec2 uv;\nuniform sampler2D screenTexture;\n\nuniform float blurRadius; // %10.0%\nuniform vec2 screenSize;       // 屏幕分辨率\nuniform float intensity; // %2.0%\nuniform vec2 center; // %0.5, 0.5%\nuniform float radius; // %0.1%\n\n// 计算基于焦点的模糊强度\nfloat computeBlurRadius(vec2 uv) {\n    // 计算当前像素位置与焦点的距离\n    float distanceToFocus = distance(uv, center);\n    \n    // 通过焦点半径控制模糊，距离焦点越远模糊越大\n    float normalizedDistance = clamp((distanceToFocus - radius) / (1.0 - radius), 0.0, 1.0);\n    \n    return blurRadius * normalizedDistance;\n}\n\n// 高斯模糊核权重\nfloat gaussian(vec2 offset, float radius) {\n    return exp(-dot(offset, offset) / (2.0 * radius * radius)) / (2.0 * 3.14159265359 * radius * radius);\n}\n\nconst vec2 rectMin = vec2(0.0, 0.0);\nconst vec2 rectMax = vec2(1.0, 1.0);\nvec4 getcolor(vec2 uv)\n{\n\tif (uv.x >= rectMin.x && uv.x <= rectMax.x && uv.y >= rectMin.y && uv.y <= rectMax.y)\n\t\treturn texture2D(screenTexture, uv);\n\telse\n\t\treturn vec4(0.0, 0.0, 0.0, 1.0);\n}\n\n// 模糊函数\nvec4 blur(vec2 uv, float radius) {\n\tif (radius <= 0.5) {\n        return getcolor(uv + vec2(0, 0));\n    }\n    vec4 color = vec4(0.0);\n    float total = 0.0;\n\n    int _rn = int(radius);\n    if (_rn < 1) _rn = 1;\n    _rn = int(float(_rn) * max(uScale, 0.25)); if (_rn < 1) _rn = 1; if (_rn > 8) _rn = 8;\n    int _span = _rn * 2 + 1;\n    for (int _xi = 0; _xi < 64; _xi++) {\n        if (_xi >= _span) break;\n        float x = float(_xi) - float(_rn);\n        for (int _yi = 0; _yi < 64; _yi++) {\n            if (_yi >= _span) break;\n            float y = float(_yi) - float(_rn);\n            vec2 offset = vec2(x, y) / screenSize;\n            float weight = gaussian(vec2(x, y), radius);\n            color += getcolor(uv + offset * intensity) * weight;\n            total += weight;\n        }\n    }\n\n    return color / total;\n}\n\n\nvoid main() {\n    float adaptiveBlurRadius = computeBlurRadius(uv); // 计算自适应模糊半径\n    vec4 blurredColor = blur(uv, adaptiveBlurRadius); // 使用自适应模糊\n    gl_FragColor = blurredColor;\n\tgl_FragColor.a = 1.0;\n}\n",
  "burn": "#version 100\nprecision highp float;\n\nvarying vec2 uv; // 替换 v_texCoord\nuniform sampler2D screenTexture; // 替换 u_texture 和 CC_Texture0\nuniform float progress; // %0.5%\nuniform vec4 burnColor; // %0.5, 0.5, 0.5, 1.0%\nuniform float time; // 替换 u_time\nuniform vec2 screenSize; // 屏幕大小\n\n// 生成伪随机数\nfloat random(vec2 co) {\n    return fract(sin(dot(co.xy, vec2(12.9898, 78.233))) * 43758.5453);\n}\n\n// 噪声函数，基于 uv 坐标生成噪声\nfloat noise(vec2 uv) {\n    vec2 p = floor(uv);\n    vec2 f = fract(uv);\n    // 使用双线性插值平滑噪声\n    float a = random(p);\n    float b = random(p + vec2(1.0, 0.0));\n    float c = random(p + vec2(0.0, 1.0));\n    float d = random(p + vec2(1.0, 1.0));\n    vec2 u = f * f * (3.0 - 2.0 * f);\n    return mix(a, b, u.x) + (c - a) * u.y * (1.0 - u.x) + (d - b) * u.x * u.y;\n}\n\nvoid main() {\n    vec2 uv = uv; // 使用新的 uv 变量\n    vec4 color = texture2D(screenTexture, uv); // 替换 CC_Texture0 为 screenTexture\n\n    // 动态噪声，使用 uv 坐标和时间生成不规则噪声\n    float burnNoise = noise(uv * 10.0 + vec2(time * 0.5, time * 0.5));\n    \n    // 控制燃烧效果的进展\n    float burnFactor = progress - burnNoise;\n\n    if (burnFactor < 0.0) {\n        // 未燃烧部分，保留原始颜色\n        gl_FragColor = color;\n        gl_FragColor.a = 1.0;\n    } else {\n        // 燃烧中的区域，渐变为燃烧颜色并逐渐透明\n        vec4 burnedColor = mix(color, burnColor, burnFactor);\n        burnedColor.a = mix(1.0, 0.0, burnFactor);\n        gl_FragColor = burnedColor;\n    }\n}\n",
  "camera": "#version 100\nprecision mediump float;\n\nvarying vec2 uv; // 材质 UV，替换v_texCoord\nuniform vec2 screenSize; // 屏幕大小resolution\nuniform sampler2D screenTexture; // 屏幕材质，替换u_texture和CC_Texture0\nuniform float time; // 时间，以秒为单位，替换u_time\n\n// 控制变量：缩放倍率、中心偏移、旋转角度\nuniform float zoom; // %1.0%\nuniform vec2 offset; // %0.0, 0.0%\nuniform float rotation; // %0.0%\n\nconst vec2 rectMin = vec2(0.0, 0.0);\nconst vec2 rectMax = vec2(1.0, 1.0);\n\nvoid main()\n{\n    vec2 centeredCoord = uv - vec2(0.5, 0.5);\n    float scale = screenSize.y / screenSize.x;\n    centeredCoord.x = centeredCoord.x / scale;\n\n    centeredCoord /= zoom;\n\n    float sinAngle = sin(rotation * 3.1415926 / 180.0);\n    float cosAngle = cos(rotation * 3.1415926 / 180.0);\n    vec2 rotatedCoord = vec2(\n        cosAngle * centeredCoord.x - sinAngle * centeredCoord.y,\n        sinAngle * centeredCoord.x + cosAngle * centeredCoord.y\n    );\n    \n    rotatedCoord.x = rotatedCoord.x * scale;\n\n    rotatedCoord = rotatedCoord - offset + vec2(0.5, 0.5);\n\t\n\tif (rotatedCoord.x >= rectMin.x && rotatedCoord.x <= rectMax.x && rotatedCoord.y >= rectMin.y && rotatedCoord.y <= rectMax.y) \n\t{\n\t\tgl_FragColor = texture2D(screenTexture, rotatedCoord);\n\t\tgl_FragColor.a = 1.0;\n\t}\n\telse\n\t{\n\t\tgl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);\n\t}\n}\n",
  "color_shift": "#version 100\nprecision mediump float;\n\nuniform sampler2D screenTexture;\nvarying vec2 uv;\n\nuniform float hueShift; // %0.0%\nuniform float saturationShift; // %0.0%\nuniform float valueShift; // %0.0%\n\n// RGB 转 HSV\nvec3 rgb2hsv(vec3 c)\n{\n    float cMax = max(c.r, max(c.g, c.b));\n    float cMin = min(c.r, min(c.g, c.b));\n    float delta = cMax - cMin;\n\n    float h = 0.0;\n    if(delta > 0.0)\n    {\n        if(cMax == c.r)\n            h = mod((c.g - c.b) / delta, 6.0);\n        else if(cMax == c.g)\n            h = (c.b - c.r) / delta + 2.0;\n        else\n            h = (c.r - c.g) / delta + 4.0;\n        h /= 6.0;\n    }\n\n    float s = (cMax > 0.0) ? (delta / cMax) : 0.0;\n    float v = cMax;\n\n    return vec3(h, s, v);\n}\n\n\n// HSV 转 RGB\nvec3 hsv2rgb(vec3 c)\n{\n    float h = c.x * 6.0;\n    float s = c.y;\n    float v = c.z;\n\n    float c1 = v * s;\n    float x = c1 * (1.0 - abs(mod(h, 2.0) - 1.0));\n    float m = v - c1;\n\n    vec3 rgb;\n\n    if(h >= 0.0 && h < 1.0)\n        rgb = vec3(c1, x, 0.0);\n    else if(h >= 1.0 && h < 2.0)\n        rgb = vec3(x, c1, 0.0);\n    else if(h >= 2.0 && h < 3.0)\n        rgb = vec3(0.0, c1, x);\n    else if(h >= 3.0 && h < 4.0)\n        rgb = vec3(0.0, x, c1);\n    else if(h >= 4.0 && h < 5.0)\n        rgb = vec3(x, 0.0, c1);\n    else\n        rgb = vec3(c1, 0.0, x);\n\n    return rgb + vec3(m);\n}\n\n\nvoid main()\n{\n    // 获取当前像素颜色\n    vec4 color = texture2D(screenTexture, uv);\n    \n    // 将 RGB 转为 HSV\n    vec3 hsv = rgb2hsv(color.rgb);\n    \n    // 调整色相\n    hsv.x += hueShift;\n    hsv.x = fract(hsv.x); // 保持色相在 [0, 1] 范围内\n\t\n\thsv.y += saturationShift;\n\thsv.y = clamp(hsv.y, 0.0, 1.0); // 限制在 [0, 1]\n\n\thsv.z += valueShift;\n\thsv.z = clamp(hsv.z, 0.0, 1.0); // 限制在 [0, 1]\n    \n    // 将 HSV 转回 RGB\n    vec3 rgb = hsv2rgb(hsv);\n    \n    // 输出最终颜色\n    gl_FragColor = vec4(rgb, 1.0);\n}\n",
  "distortion": "#version 100\nprecision mediump float;\n\nvarying vec2 uv; // 替换 v_texCoord\nuniform sampler2D screenTexture; // 替换 u_texture 和 CC_Texture0\nuniform float progress; // %0.5%\nuniform float power; // %10.0%\nuniform vec2 screenSize; // 替换 resolution\nuniform float time; // 替换 u_time\n\nvoid main() \n{\n    // 图像中心坐标\n    vec2 center = vec2(0.5, 0.5);\n    \n    // 计算当前纹理坐标到中心的向量\n    vec2 direction = uv - center;\n    \n    // 计算距离与方向的旋转角度\n    float distance = length(direction);\n    \n    // 控制扭曲效果，progress 控制强度\n    float angle = progress * distance * power; \n    \n    // 计算旋转后的纹理坐标\n    float sinAngle = sin(angle);\n    float cosAngle = cos(angle);\n    vec2 twistedCoord = vec2(\n        direction.x * cosAngle - direction.y * sinAngle,\n        direction.x * sinAngle + direction.y * cosAngle\n    );\n    \n    // 应用扭曲后的坐标\n    vec2 finalUV = center + twistedCoord;\n    \n    // 从屏幕材质中采样颜色\n    vec4 color = texture2D(screenTexture, finalUV);\n    \n    // 设置输出颜色，保持透明度为 1.0\n    gl_FragColor = color;\n    gl_FragColor.a = 1.0;\n}\n",
  "emboss": "#version 100\nprecision mediump float;\n\nvarying vec2 uv; // 材质 UV\nuniform sampler2D screenTexture; // 屏幕材质\nuniform vec2 screenSize;\nuniform float strength; // %1.0%\nuniform float time; // 时间，以秒为单位\n\nvec4 getcolor(vec2 uv)\n{\n    return texture2D(screenTexture, uv);\n}\n\nvoid main()\n{\n    // 获取当前 UV 坐标\n    vec2 uv = uv;\n    vec4 originalColor = texture2D(screenTexture, uv);\n    \n    // 获取纹理像素的偏移量\n    vec2 offset = vec2(1.0 / screenSize.x, 1.0 / screenSize.y);\n\n    // 获取周围的像素颜色\n    vec4 colorTL = getcolor(uv + vec2(-offset.x, -offset.y)); // 左上\n    vec4 colorBR = getcolor(uv + vec2(offset.x, offset.y));   // 右下\n\n    // Emboss 卷积核计算\n    vec4 embossColor = colorTL - colorBR;\n\n    // 调整对比度\n    embossColor += vec4(0.5, 0.5, 0.5, 1.0);\n    \n    // 将颜色值限制在 [0.0, 1.0] 范围内\n    embossColor = clamp(embossColor, 0.0, 1.0);\n    \n    // 混合原始颜色与 Emboss 效果颜色\n    vec4 finalColor = mix(originalColor, embossColor, strength);\n    \n    // 设置最终颜色\n    gl_FragColor = finalColor;\n    gl_FragColor.a = 1.0;\n}\n",
  "flare": "#version 100\nprecision mediump float;\n\nuniform vec2 lightpos; // %0.5, 0.5%\nuniform float power; // %0.01%\n\nvarying vec2 uv; // 纹理坐标\nuniform sampler2D screenTexture; // 输入的纹理\nuniform vec2 screenSize; // 屏幕分辨率\nuniform float time; // 时间\n\nvoid main()\n{\n    vec2 norm_uv = (uv * screenSize - screenSize * 0.5) / screenSize.y;\n    vec2 norm_lightpos = (lightpos * screenSize - screenSize * 0.5) / screenSize.y;\n\n    // 采样屏幕纹理颜色\n    vec4 color = texture2D(screenTexture, uv);\n\n    // 计算当前像素与光源位置的距离\n    float distance = length(norm_uv - norm_lightpos);\n\n    // 根据距离计算炫光效果，距离越近效果越强\n    float glow = power / (distance * distance);\n\n    // 混合炫光效果和原始颜色\n    vec3 finalColor = color.rgb + vec3(glow);\n\n    // 输出最终颜色\n    gl_FragColor = vec4(finalColor, 1.0);\n}\n",
  "flip": "#version 100\nprecision mediump float;\n\n// 传入的方向：0 表示水平翻转，1 表示垂直翻转\nuniform float direction; // %0.0%\n// 翻转进度，0 表示未翻转，1 表示翻转完成\nuniform float progress; // %1.0%\n// 纹理\nuniform sampler2D screenTexture; // 替换u_texture和CC_Texture0\n// 纹理坐标\nvarying vec2 uv; // 替换v_texCoord\n\nvoid main() {\n    vec2 uvCoord = uv;\n\t\n\tif (direction == 0.0) {\n        // 水平翻转\n        // 在 progress 为 0 时保持正常, progress 为 1 时完全翻转\n\t\tfloat adjusted_x;\n\n\t\tif (progress < 0.5) {\n\t\t\tadjusted_x = 0.5 + (uvCoord.x - 0.5) / (0.5 - progress);\n\t\t} else if (progress == 0.5) {\n\t\t\tadjusted_x = (uvCoord.x < 0.5) ? -1.0 / 0.0 : 1.0 / 0.0; \n\t\t} else {\n\t\t\tadjusted_x = mix(0.5 + (uvCoord.x - 0.5) / (0.5 - progress), 1.0 - uvCoord.x, (progress - 0.5) * 2.0);\n\t\t}\n\n\t\tuvCoord.x = mix(uvCoord.x, adjusted_x, progress);\n    } else {\n        // 垂直翻转\n        // 在 progress 为 0 时保持正常, progress 为 1 时完全翻转\n\t\tfloat adjusted_y;\n\n\t\tif (progress < 0.5) {\n\t\t\tadjusted_y = 0.5 + (uvCoord.y - 0.5) / (0.5 - progress);\n\t\t} else if (progress == 0.5) {\n\t\t\tadjusted_y = (uvCoord.y < 0.5) ? -1.0 / 0.0 : 1.0 / 0.0; \n\t\t} else {\n\t\t\tadjusted_y = mix(0.5 + (uvCoord.y - 0.5) / (0.5 - progress), 1.0 - uvCoord.y, (progress - 0.5) * 2.0);\n\t\t}\n\t\t\n\t\tuvCoord.y = mix(uvCoord.y, adjusted_y, progress);\n    }\n\t\n\tvec4 color = texture2D(screenTexture, uvCoord); // 替换CC_Texture0为screenTexture\n\tgl_FragColor = color;\n\tgl_FragColor.a = 1.0;\n}\n",
  "flowing": "#version 100\nprecision mediump float;\n\nuniform sampler2D screenTexture;\nuniform float time;\nuniform vec2 screenSize;\n\nvarying vec2 uv;\n\nuniform float speed; // %1.0%\nuniform float power; // %1.0%\nuniform float width; // %0.2%\nuniform vec4 color; // %1.0, 1.0, 0.5, 1.0%\nuniform vec2 direction; // %1.0, 1.0%\n\nvoid main() {\n    vec3 lightColor = color.rgb;\n    float lightPos = mod(time * speed, 1.0);\n    float halfWidth = width / 2.0;\n\n    float dist = abs(dot(uv - vec2(lightPos, lightPos), direction));\n\n    float intensity = smoothstep(halfWidth, 0.0, dist) * power;\n\n    vec4 texColor = texture2D(screenTexture, uv);\n\n    vec3 resultColor = texColor.rgb + lightColor * intensity;\n\n    gl_FragColor = vec4(clamp(resultColor, 0.0, 1.0), 1.0);\n}\n",
  "fog": "#version 100\nprecision mediump float;\n\nvarying vec2 uv;\nuniform sampler2D screenTexture;\n\nuniform vec4 fogColor; // %0.8, 0.8, 0.8, 1.0%\nuniform float fogStart; // %0.8%\nuniform float fogEnd; // %-0.2%\nuniform float time;\n\nvoid main() {\n    // 读取纹理颜色\n    vec4 color = texture2D(screenTexture, uv);\n\n    // 使用Y坐标计算雾的基础浓度\n    float fogFactor = clamp((fogEnd - uv.y) / (fogEnd - fogStart), 0.0, 1.0);\n\n    // 将雾的颜色与原始颜色混合\n    vec4 finalColor = mix(fogColor, color, fogFactor);\n\n    gl_FragColor = vec4(finalColor.rgb, 1.0); // 最终颜色，使用原始透明度\n}\n",
  "glow_effect": "#version 100\nprecision mediump float;\n\nvarying vec2 uv; // 替换v_texCoord\nuniform sampler2D screenTexture; // 替换CC_Texture0和u_texture\nuniform vec2 screenSize; // 替换resolution\nuniform float time; // 替换u_time\n\nuniform float intensity; // %0.6%\nuniform float threshold; // %0.3%\n\nvec4 getcolor(vec2 uv) {\n    return texture2D(screenTexture, uv);\n}\n\nfloat calculateLuminance(vec3 color) {\n    return dot(color, vec3(0.2126, 0.7152, 0.0722));\n}\n\nvoid main() {\n    vec3 color = texture2D(screenTexture, uv).rgb; // 替换texture2D(CC_Texture0, uv)\n\n    float luminance = calculateLuminance(color);\n\n    vec3 bloom = vec3(0.0);\n    \n    if (luminance > threshold) {\n        for (int x = -2; x <= 2; x++) {\n            for (int y = -2; y <= 2; y++) {\n                vec2 offset = vec2(float(x), float(y)) / screenSize; // 替换resolution为screenSize\n                vec3 sampleColor = getcolor(uv + offset).rgb;\n                bloom += sampleColor;\n            }\n        }\n        bloom /= 25.0;\n        bloom *= intensity;\n    }\n    \n    vec3 finalColor = color + bloom;\n    \n    gl_FragColor = vec4(finalColor, 1.0);\n}\n",
  "heat_distortion": "#version 100\nprecision highp float;\n\nvarying vec2 uv;\nuniform sampler2D screenTexture;       // 背景纹理\nuniform float time;                // 用于动画的时间参数\nuniform float strength; // %0.03%\nuniform float frequency; // %1.0%\n\n// 生成噪声函数（类似于简单的Perlin噪声）\nfloat rand(vec2 co)\n{\n    return fract(sin(dot(co.xy ,vec2(12.9898, 78.233))) * 43758.5453);\n}\n\n// 生成二维噪声\nfloat noise(vec2 pos)\n{\n    vec2 i = floor(pos);\n    vec2 f = fract(pos);\n    \n    // 使用不同的随机种子生成不同的噪声值\n    float a = rand(i);\n    float b = rand(i + vec2(1.0, 0.0));\n    float c = rand(i + vec2(0.0, 1.0));\n    float d = rand(i + vec2(1.0, 1.0));\n    \n    // 进行插值来平滑噪声\n    vec2 u = f*f*(3.0-2.0*f);\n    \n    return (mix(a, b, u.x) + (c - a)* u.y * (1.0 - u.x) + (d - b) * u.x * u.y) * 2.0 - 1.0;\n}\n\nconst vec2 rectMin = vec2(0.0, 0.0);\nconst vec2 rectMax = vec2(1.0, 1.0);\n\nvoid main()\n{\n\tvec4 oriColor = texture2D(screenTexture, uv);\n\t\n\t\n    // 使用时间和纹理坐标生成动态噪声\n    vec2 noiseCoord = uv * 5.0 + vec2(time * frequency, time * frequency);  // 缩放噪声纹理\n    float noiseValue = noise(noiseCoord);\n    \n    // 使用噪声值生成扰动\n    vec2 newuv = uv + vec2(noiseValue) * strength;\n\n\tif (newuv.x >= rectMin.x && newuv.x <= rectMax.x && newuv.y >= rectMin.y && newuv.y <= rectMax.y) \n\t{\n\t\tgl_FragColor = texture2D(screenTexture, newuv);\n\t}\n\telse\n\t{\n\t\tgl_FragColor = vec4(0, 0, 0, 1);\n\t}\n\tgl_FragColor.a = 1.0;\n}\n",
  "hologram": "#version 100\nprecision highp float;\n\nvarying vec2 uv; // 替换 v_texCoord\nuniform sampler2D screenTexture; // 替换 u_texture 和 CC_Texture0\nuniform float time; // 替换 u_time\nuniform vec4 hologramColor; // %0.0, 1.0, 1.5, 1.0%\nuniform vec2 screenSize; // 屏幕大小\n\nvec4 getcolor(vec2 uv)\n{\n    return texture2D(screenTexture, uv); // 替换 CC_Texture0\n}\n\nvoid main() {\n    vec2 uv = uv; // 使用新的uv变量\n    \n    // 颜色分离模拟 RGB 光的衍射效果\n    float offset = sin(uv.y * 40.0 + time * 5.0) * 0.004;\n    vec4 color;\n    color.r = getcolor(uv + vec2(offset, 0.0)).r;\n    color.g = getcolor(uv).g; // 中间通道不需要偏移\n    color.b = getcolor(uv - vec2(offset, 0.0)).b;\n    \n    // 加入扫描线效果\n    float scanline = sin((uv.y + time * 0.5) * 100.0) * 0.1;\n    color.rgb -= scanline;\n    \n    // 应用 Hologram 颜色调制\n    color *= hologramColor;\n\n    // 模拟全息静电噪声\n    float noise = fract(sin(dot(uv * time, vec2(12.9898, 78.233))) * 43758.5453);\n    color.rgb += noise * 0.05;\n    \n    // 输出最终效果\n    gl_FragColor = color;\n    gl_FragColor.a = 1.0;\n}\n",
  "image_noise": "#version 100\nprecision highp float;\n\nvarying lowp vec2 uv;\nuniform sampler2D screenTexture;\nuniform float time;\n\nuniform float power; // %0.2%\nuniform float density; // %400.0%\n\nfloat rand(vec2 co) {\n    return fract(sin(dot(co.xy * time * 0.05, vec2(12.9898, 78.233))) * 43758.5453);\n}\n\nfloat noise(vec2 p) {\n    vec2 ip = floor(p);\n    vec2 u = fract(p);\n    u = u*u*(3.0-2.0*u);\n    \n    float res = mix(\n        mix(rand(ip), rand(ip + vec2(1.0, 0.0)), u.x),\n        mix(rand(ip + vec2(0.0, 1.0)), rand(ip + vec2(1.0, 1.0)), u.x),\n        u.y\n    );\n    return res;\n}\n\nvoid main() {\n    vec4 color = texture2D(screenTexture, uv);\n    \n    // 噪点随时间变化，加入uv扰动\n    float noisyFactor = noise(uv * density + time * 0.1) * 2.0 - 1.0;\n    \n    // 添加噪点\n    vec4 noisyColor = color + vec4(vec3(noisyFactor) * power, 0.0);\n    \n    gl_FragColor = noisyColor;\n    gl_FragColor.a = 1.0;\n}",
  "kaleidoscope": "#version 100\nprecision mediump float;\n\nvarying vec2 uv;\nuniform sampler2D screenTexture;\n\nuniform float segments; // %6.0%\nuniform vec2 center; // %0.5, 0.5%\nuniform float angle; // %0.0%\n\nvoid main() {\n    vec2 coord = uv;\n\t\n\t// 转换坐标到中心点\n    coord = coord - center;\n    \n\t// 计算角度并加入旋转\n    float theta = atan(coord.y, coord.x) + angle;\n    \n\t// 计算半径\n    float radius = length(coord);\n\n    // 计算角度映射到分段的区域\n    float segAngle = 3.1415926 * 2.0 / segments;\n    theta = mod(theta, segAngle);\n    theta = abs(theta - segAngle / 2.0);  // 镜像变换\n\n    // 重新计算 UV 坐标\n    vec2 newUV = vec2(cos(theta), sin(theta)) * radius + center;\n\t\n\t// 读取纹理颜色\n    vec4 color = texture2D(screenTexture, newUV);\n    \n\t// 设置最终的颜色和透明度\n    gl_FragColor = color;\n    gl_FragColor.a = 1.0;\n}\n",
  "lightning": "#version 100\nprecision highp float;\nuniform float uScale;\n\nvarying vec2 uv;\n\nuniform float time;\nuniform sampler2D screenTexture;\n\nuniform float numBolts; // %10.0%\nuniform float width; // %0.003%\nuniform float flashDuration; // %1.0%\nuniform float amplitude; // %0.3%\nuniform float frequency; // %2.0%\n\nfloat rand(vec2 co)\n{\n    return fract(sin(dot(co.xy ,vec2(12.9898,78.233))) * 43758.5453);\n}\n\nfloat noise(float x, float seed)\n{\n    float i = floor(x);\n    float f = fract(x);\n    float u = f * f * (3.0 - 2.0 * f);\n    float a = rand(vec2(i, seed));\n    float b = rand(vec2(i + 1.0, seed));\n    return mix(a, b, u);\n}\n\nfloat randomPath(float y, float seed)\n{\n    float x = 0.0;\n    float _amplitude = amplitude; // 总的偏移幅度\n    float _frequency = frequency;  // 初始频率\n\n    // 叠加多层噪声\n    for(int i = 0; i < 10; i++)\n    {\n        float n = noise(y * _frequency, seed + float(i) * 10.0);\n        x += (n - 0.5) * _amplitude;\n\n        // 递增频率，递减幅度\n        _frequency *= 2.0;\n        _amplitude *= 0.5;\n    }\n\n    // 将 x 限制在 [0,1] 范围内\n    x = clamp(x + rand(vec2(seed, seed)), 0.0, 1.0);\n\n    return x;\n}\n\nvoid main()\n{\n    vec4 color = texture2D(screenTexture, uv);\n\n    float totalTime = time;\n    float seedTime = floor(totalTime / flashDuration);\n    float seed = seedTime;\n    float flashTime = fract(totalTime / flashDuration) * flashDuration;\n    float fade = 1.0 - (flashTime / flashDuration);\n    fade = pow(fade, 2.0);\n\n    float lightning = 0.0;\n\n    int _nb = int(numBolts);\n    if (_nb < 1) _nb = 1;\n    _nb = int(float(_nb) * max(uScale, 0.25)); if (_nb < 1) _nb = 1; if (_nb > 32) _nb = 32;\n    for(int _i = 0; _i < 64; _i++)\n    {\n        if (_i >= _nb) break;\n        float i = float(_i);\n        float boltSeed = seed + i * 100.0;\n\n        // 调用新的 randomPath 函数\n        float x = randomPath(uv.y, boltSeed);\n\n        float dist = abs(uv.x - x);\n\n        lightning += smoothstep(width, 0.0, dist);\n    }\n\n    lightning = clamp(lightning, 0.0, 1.0);\n    lightning *= fade;\n\n    vec4 lightningColor = vec4(1.0, 1.0, 1.0, lightning);\n    gl_FragColor = mix(color, lightningColor, lightning);\n\tgl_FragColor.a = 1.0;\n}",
  "liquid": "#version 100\nprecision mediump float;\n\nuniform sampler2D screenTexture;  // 替换u_texture和CC_Texture0\nuniform float time;               // 替换u_time\nuniform float frequency; // %10.0%\nuniform float amplitude; // %0.02%\nuniform vec2 screenSize;          // 屏幕大小\n\nvarying vec2 uv;                  // 替换v_texCoord\n\nvoid main()\n{\n    vec2 offset = vec2(sin(uv.y * frequency + time) * amplitude, cos(uv.x * frequency + time) * amplitude);\n\n    vec2 distortedUV = uv + offset;\n\n    vec4 color = texture2D(screenTexture, distortedUV);\n\n    gl_FragColor = color;\n    gl_FragColor.a = 1.0;\n}\n",
  "neon": "#version 100\nprecision mediump float;\n\nvarying vec2 uv;\nuniform sampler2D screenTexture;\nuniform vec2 screenSize;\n\nuniform float threshold; // %0.9%\nuniform vec4 color; // %0.0, 1.0, 1.0, 1.0%\n\nvec4 getcolor(vec2 uv)\n{\n\treturn texture2D(screenTexture, uv);\n}\n\nvoid main()\n{\n    // Sobel 核\n    float kernelX[9];\n    float kernelY[9];\n\n    kernelX[0] = -1.0; kernelX[1] = 0.0; kernelX[2] = 1.0;\n    kernelX[3] = -2.0; kernelX[4] = 0.0; kernelX[5] = 2.0;\n    kernelX[6] = -1.0; kernelX[7] = 0.0; kernelX[8] = 1.0;\n\n    kernelY[0] = -1.0; kernelY[1] = -2.0; kernelY[2] = -1.0;\n    kernelY[3] = 0.0;  kernelY[4] = 0.0;  kernelY[5] = 0.0;\n    kernelY[6] = 1.0;  kernelY[7] = 2.0;  kernelY[8] = 1.0;\n\n    vec2 texOffset[9];\n    texOffset[0] = vec2(-1.0, -1.0) / screenSize;\n    texOffset[1] = vec2( 0.0, -1.0) / screenSize;\n    texOffset[2] = vec2( 1.0, -1.0) / screenSize;\n    texOffset[3] = vec2(-1.0,  0.0) / screenSize;\n    texOffset[4] = vec2( 0.0,  0.0) / screenSize;\n    texOffset[5] = vec2( 1.0,  0.0) / screenSize;\n    texOffset[6] = vec2(-1.0,  1.0) / screenSize;\n    texOffset[7] = vec2( 0.0,  1.0) / screenSize;\n    texOffset[8] = vec2( 1.0,  1.0) / screenSize;\n\n    float sumX = 0.0;\n    float sumY = 0.0;\n\t\n\tvec3 originalColor = texture2D(screenTexture, uv).rgb;\n\n    for (int i = 0; i < 9; i++) {\n        vec4 _color = getcolor(uv + texOffset[i]);\n        float intensity = dot(_color.rgb, vec3(0.299, 0.587, 0.114));  // 转为灰度\n        sumX += intensity * kernelX[i];\n        sumY += intensity * kernelY[i];\n    }\n\n    float sobelValue = sqrt(sumX * sumX + sumY * sumY);\n\t\n\tif (sobelValue > threshold){\n\t\tgl_FragColor = color;\n\t}\n\telse{\n\t\tgl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);\n\t}\n}\n",
  "night_vision": "#version 100\nprecision highp float;\n\nvarying vec2 uv;\n\nuniform sampler2D screenTexture;\nuniform vec2 screenSize;\nuniform float time;\nuniform float power; // %1.5%\n\n// 生成随机噪声\nfloat random(vec2 uv) {\n    return fract(sin(dot(uv.xy, vec2(12.9898, 78.233))) * 43758.5453123);\n}\n\nvoid main() {\n    vec2 uv = uv;\n    vec4 color = texture2D(screenTexture, uv);\n\n    // 将颜色转换为灰度\n    float gray = dot(color.rgb, vec3(0.299, 0.587, 0.114));\n\n    // 添加绿色滤镜\n    vec3 nightVisionColor = vec3(0.1, 1.0, 0.1) * gray;\n\n    // 添加随机噪点\n    float noise = random(uv + time * 0.1);\n    nightVisionColor += noise * 0.1;\n\n    // 使用传入的 power 变量来增强亮度\n    nightVisionColor = clamp(nightVisionColor * power, 0.0, 1.0);\n\n    // 计算当前像素到屏幕中心的距离\n    vec2 center = vec2(0.5, 0.5);\n    float dist = distance(uv, center);\n\n    // 控制 vignette 渐晕效果的强度和大小\n    float vignette = smoothstep(0.4, 0.8, dist);\n\n    // 将 vignette 效果应用到颜色上，边缘颜色变暗\n    nightVisionColor *= (1.0 - vignette * 0.8);\n\n    // 输出最终的颜色\n    gl_FragColor = vec4(nightVisionColor, 1.0);\n}\n",
  "oil_painting": "#version 100\nprecision mediump float;\nuniform float uScale;\n\nvarying vec2 uv; // 材质 UV\nuniform sampler2D screenTexture; // 屏幕材质\nuniform float radius; // %0.01%\nuniform vec2 screenSize; // 屏幕大小\nuniform float sampleCount; // %5%\n\nvec4 getcolor(vec2 uv)\n{\n    return texture2D(screenTexture, uv);\n}\n\nvoid main()\n{\n    // 计算步长，基于采样次数\n    float stepW = radius / sampleCount;\n    float stepH = radius / sampleCount;\n\n    // 存储采样颜色\n    vec3 colorSum = vec3(0.0);\n    vec3 maxColor = vec3(0.0);\n    int maxCount = 0;\n\n    // 模拟多个方向上的采样，创造出模糊的“笔触”效果\n    int _sn = int(sampleCount);\n    if (_sn < 1) _sn = 1;\n    _sn = int(float(_sn) * max(uScale, 0.25)); if (_sn < 1) _sn = 1; if (_sn > 8) _sn = 8;\n    int _span = _sn * 2 + 1;\n    float _total = float(_span * _span);\n    for (int _ii = 0; _ii < 64; _ii++)\n    {\n        if (_ii >= _span) break;\n        float i = float(_ii) - float(_sn);\n        for (int _jj = 0; _jj < 64; _jj++)\n        {\n            if (_jj >= _span) break;\n            float j = float(_jj) - float(_sn);\n            // 计算采样的偏移量\n            vec2 offset = vec2(i * stepW, j * stepH);\n            vec3 sampleColor = getcolor(uv + offset).rgb;\n\n            // 累计颜色\n            colorSum += sampleColor;\n\n            // 记录出现最多的颜色\n            int count = 0;\n            for (int _mm = 0; _mm < 3; _mm++)\n            {\n                for (int _nn = 0; _nn < 3; _nn++)\n                {\n                    float m = float(_mm) - 1.0;\n                    float n = float(_nn) - 1.0;\n                    vec3 compareColor = getcolor(uv + vec2(m * stepW, n * stepH)).rgb;\n                    if (distance(sampleColor, compareColor) < 0.1)\n                    {\n                        count++;\n                    }\n                }\n            }\n\n            if (count > maxCount)\n            {\n                maxCount = count;\n                maxColor = sampleColor;\n            }\n        }\n    }\n\n    // 将最多颜色与整体颜色混合，形成油画效果\n    vec3 finalColor = mix(colorSum / _total, maxColor, 0.6);\n    gl_FragColor = vec4(finalColor, 1.0);\n}\n",
  "old_tv": "#version 100\nprecision highp float;\n\nvarying vec2 uv;\nuniform sampler2D screenTexture;\nuniform float time;\n\nconst float noiseIntensity = 0.2;\nconst float stripeIntensity = 800.0;\nconst float sepiaIntensity = 0.5;\nconst mat3 sepia = mat3(\n    0.393, 0.349, 0.272,\n    0.769, 0.686, 0.534,\n    0.189, 0.168, 0.131);\n\nconst float flickerFrequency = 0.0;  // Flicker frequency\n\nvec4 getcolor(vec2 uv)\n{\n\treturn texture2D(screenTexture, uv);\n}\n// Generate dynamic noise for TV static\nfloat dynamicNoise(vec2 uv, float time)\n{\n    return fract(sin(dot(uv + time, vec2(12.9898, 78.233))) * 43758.5453);\n}\n\n// Simulate a vignette effect (darkening near the edges)\nfloat vignette(vec2 uv)\n{\n    vec2 dist = uv - vec2(0.5);\n    return smoothstep(0.7, 0.1, dot(dist, dist) * 0.8);\n}\n\n// Apply chromatic aberration by slightly offsetting color channels\nvec3 chromaticAberration(vec2 uv)\n{\n    float offset = 0.002;  // Amount of color misalignment\n    float r = getcolor(uv + vec2(offset, 0.0)).r;\n    float g = getcolor(uv + vec2(0,0)).g;\n    float b = getcolor(uv + vec2(offset, 0.0)).b;\n    return vec3(r, g, b);\n}\n\n// Screen flicker effect (modulates brightness over time)\nfloat screenFlicker(float time)\n{\n    return 0.9 + 0.1 * sin(time * flickerFrequency);  // Adjust intensity here\n}\n\n\nvoid main()\n{\n\tvec4 color = texture2D(screenTexture, uv);\n  \n    // Apply chromatic aberration\n    vec3 colorWithCA = chromaticAberration(uv);\n    \n    // Add dynamic noise\n    float n = dynamicNoise(uv, time * 10.0);\n    vec3 result = mix(colorWithCA, vec3(n), noiseIntensity);\n    \n    // Apply scanline effect\n    vec2 sc = vec2((sin(uv.y * stripeIntensity) + 1.0) / 2.0, (cos(uv.y * stripeIntensity) + 1.0) / 2.0);\n    result += colorWithCA * sc.xyx;\n    \n    // Apply sepia tone\n    mat3 sepiaDiff = mat3(1.0) + sepiaIntensity * (sepia - mat3(1.0));\n    result = sepiaDiff * result;\n    \n    // Add vignette effect\n    result *= vignette(uv);\n    \n    // Screen flicker effect (brightness modulation)\n    result *= screenFlicker(time);\n    \n\t\n\tfloat down = -time * 0.5;\n    float up = -time * 0.5 + 0.15;\n    down = mod(down, 1.0);\n    up = mod(up, 1.0);\n    \n    if (up >= down && uv.y == clamp(uv.y, down, up))\n        result = mix(result, vec3(0.0), 0.2);\n    \n    if (up < down && (uv.y <= up || uv.y >= down))\n        result = mix(result, vec3(0.0), 0.2);\n    \n    // Apply final color\n    gl_FragColor = vec4(result, 1.0);\n}\n",
  "rain": "#version 100\nprecision highp float;\nuniform float uScale;\n\nvarying vec2 uv;          // 材质 UV\nuniform vec2 screenSize;  // 屏幕大小\nuniform sampler2D screenTexture;  // 屏幕材质\nuniform float time;       // 时间，以秒为单位\nuniform float density; // %500%\nuniform float width; // %0.0005%\nuniform vec4 rainColor; // %0.5, 0.5, 1.0, 0.5%\nuniform float height; // %0.02%\n\n// 随机生成函数，用于生成雨滴的位置和分布\nfloat random(vec2 st) {\n    return fract(sin(dot(st.xy, vec2(12.9898, 78.233))) * 43758.5453123);\n}\n\n// 生成雨滴的位置\nvec2 generateDropPosition(float index, float timeOffset) {\n    float x = random(vec2(index, time + timeOffset + 10.0)) * screenSize.x; // 随机横向位置\n    float y = mod(random(vec2(index, timeOffset)) * screenSize.y - time * 2000.0, screenSize.y); // 雨滴竖向下落\n    return vec2(x, y);\n}\n\n// 主程序\nvoid main() {\n    vec2 uvCoord = uv;  // 归一化屏幕坐标\n    vec4 bgColor = texture2D(screenTexture, uvCoord);\n\n    float dropWidth = width;   // 雨滴的宽度\n    float dropHeight = height; // 雨滴的高度\n\n    // 遍历生成 density 个雨滴\n    // 遍历生成 density 个雨滴\n    float dropPresent = 0.0;\n    int _dn = int(density);\n    if (_dn < 1) _dn = 1;\n    _dn = int(float(_dn) * max(uScale, 0.25)); if (_dn < 1) _dn = 1; if (_dn > 256) _dn = 256;\n    for (int _i = 0; _i < 512; _i++) {\n        if (_i >= _dn) break;\n        float i = float(_i);\n        vec2 dropPos = generateDropPosition(i, i * 10.0);\n\n        // 检查当前像素是否属于该雨滴\n        if (abs(gl_FragCoord.x - dropPos.x) < dropWidth * screenSize.x && abs(gl_FragCoord.y - dropPos.y) < dropHeight * screenSize.y) {\n            dropPresent += 1.0;  // 如果当前像素属于雨滴，计数加1\n        }\n    }\n\n    // 如果当前像素属于任意雨滴，将雨滴颜色叠加到背景上\n    if (dropPresent > 0.0) {\n        vec4 rainOverlay = rainColor;\n        gl_FragColor = mix(bgColor, rainOverlay, rainOverlay.a); // 将雨滴叠加到背景上\n        gl_FragColor.a = 1.0;\n    } else {\n        gl_FragColor = bgColor;  // 如果不属于雨滴，保持背景图片不变\n        gl_FragColor.a = 1.0;\n    }\n}\n",
  "rainbow": "#version 100\nprecision highp float;\nvarying vec2 uv;\nuniform sampler2D screenTexture;\nuniform float time;\nconst float hueSpeed = 0.5;\nvec3 rgb2hsv(vec3 c) {\n  vec4 K = vec4(0.0, -1.0/3.0, 2.0/3.0, -1.0);\n  vec4 p = mix(vec4(c.bg, K.wz), vec4(c.gb, K.xy), step(c.b, c.g));\n  vec4 q = mix(vec4(p.xyw, c.r), vec4(c.r, p.yzx), step(p.x, c.r));\n  float d = q.x - min(q.w, q.y);\n  float e = 1.0e-10;\n  return vec3(abs(q.z + (q.w - q.y) / (6.0 * d + e)), d / (q.x + e), q.x);\n}\nvec3 hsv2rgb(vec3 c) {\n  vec3 p = abs(fract(c.xxx + vec3(0.0, 2.0/3.0, 1.0/3.0)) * 6.0 - 3.0);\n  return c.z * mix(vec3(1.0), clamp(p - 1.0, 0.0, 1.0), c.y);\n}\nvoid main() {\n  vec4 color = texture2D(screenTexture, uv);\n  vec3 hsv = rgb2hsv(color.rgb);\n  hsv.x = fract(hsv.x + time * hueSpeed);\n  vec3 rgb = hsv2rgb(hsv);\n  gl_FragColor = vec4(rgb, color.a);\n}",
  "rays": "#version 100\nprecision mediump float;\nuniform float uScale;\n\nvarying vec2 uv;\n\nuniform sampler2D screenTexture; // 场景纹理\n\nuniform vec2 lightpos; // %0.5, 0.5%\nuniform float exposure; // %0.5%\nuniform float decay; // %0.95%\nuniform float density; // %0.5%\nuniform float weight; // %0.3%\n\nconst vec2 rectMin = vec2(0.0, 0.0);\nconst vec2 rectMax = vec2(1.0, 1.0);\nvec4 getcolor(vec2 uv)\n{\n\tif (uv.x >= rectMin.x && uv.x <= rectMax.x && uv.y >= rectMin.y && uv.y <= rectMax.y)\n\t\treturn texture2D(screenTexture, uv);\n\telse\n\t\treturn vec4(0.0, 0.0, 0.0, 1.0);\n}\n\n\nvoid main()\n{\n    // 从纹理中获取当前像素的颜色\n    vec4 color = texture2D(screenTexture, uv);\n\n    // 光源到当前像素的方向\n    vec2 deltaTexCoord = uv - lightpos;\n\n    // 迭代次数决定光束的长度\n    float illuminationDecay = 1.0;\n    vec4 godRays = vec4(0.0);\n\n    // 采样次数，可以根据需要调整\n    int samples = int(100.0 * uScale); if (samples < 1) samples = 1;\n\t\n\tvec2 _uv = uv;\n\n    for (int i = 0; i < 128; ++i) { if (i >= samples) break;\n        // 沿光束方向逐渐采样\n        _uv -= deltaTexCoord * density;\n        vec4 sampleColor = getcolor(_uv);\n\n        // 逐步衰减\n        sampleColor *= illuminationDecay * weight;\n\n        // 累加结果\n        godRays += sampleColor;\n\n        // 计算衰减\n        illuminationDecay *= decay;\n    }\n\n    // 将 God Rays 叠加到原始颜色上\n    color += godRays * exposure;\n\n    gl_FragColor = color;\n\tgl_FragColor.a = 1.0;\n}\n",
  "scanline": "#version 100\nprecision highp float;\n\nvarying vec2 uv;\n\nuniform sampler2D screenTexture; // 屏幕材质\nuniform vec2 screenSize; // 屏幕大小\nuniform float time; // 时间\nuniform float power; // %0.5%\n\nvoid main()\n{\n    // 读取当前像素的颜色\n    vec4 color = texture2D(screenTexture, uv);\n    \n    // 获取当前像素的y坐标（范围是0到1）\n    float scanline = sin((uv.y * 800.0 + time * 5.0) * 3.14159 * 2.0); // 模拟扫描线\n    float intensity = 1.0 - power * (scanline * 0.5 + 0.5);\n    \n    // 将扫描线的效果应用到颜色上\n    color.rgb *= intensity;\n    \n    // 设置透明度\n    gl_FragColor = color;\n    gl_FragColor.a = 1.0;\n}\n",
  "snow": "#version 100\nprecision highp float;\nuniform float uScale;\n\nvarying vec2 uv;\nuniform sampler2D screenTexture;\nuniform float time;\nuniform vec2 screenSize;\nuniform float speedx; // %20.0%\nuniform float speedy; // %60.0%\nuniform float snowCount; // %400.0%\nuniform float size; // %0.014%\nuniform vec4 color; // %1.0, 1.0, 1.0, 0.0%\n\n// 随机函数，用于生成雪花随机位置\nfloat random(vec2 st) {\n    return fract(sin(dot(st.xy, vec2(12.9898, 78.233))) * 43758.5453123);\n}\n\n// 模拟雪花飘落\nvoid main() {\n    // 获取背景颜色\n    vec4 bgColor = texture2D(screenTexture, uv);\n\n    // 初始化颜色\n    vec4 snowColor = color; // 白色雪花，透明度为0表示未覆盖\n    bool cover = false;\n\n    // 遍历雪花\n    int _sn2 = int(snowCount);\n    if (_sn2 < 1) _sn2 = 1;\n    _sn2 = int(float(_sn2) * max(uScale, 0.25)); if (_sn2 < 1) _sn2 = 1; if (_sn2 > 256) _sn2 = 256;\n    for (int _i = 0; _i < 512; _i++) {\n        if (_i >= _sn2) break;\n        float fi = float(_i);\n        // 基于时间和索引生成随机雪花位置\n        float xRandom = random(vec2(fi, fi)) * screenSize.x;\n        float yRandom = random(vec2(fi, fi)) * screenSize.y;\n\n        // 雪花的初始位置\n        vec2 snowPos = vec2(\n            mod(xRandom + sin(time + fi) * speedx * (1.0 + random(vec2(fi, fi)) / 5.0), screenSize.x), // 基于传入的横向速度\n            mod(screenSize.y - mod(time * speedy + yRandom + fi * 30.0, screenSize.y), screenSize.y) * (1.0 + random(vec2(fi + 1.0, fi + 1.0)) / 5.0) // 基于传入的纵向速度\n        );\n\n        float baseSize = size; // 基础大小\n        float sizeVariation = 0.004; // 大小变化幅度\n        float flakeSize = baseSize + sizeVariation * sin(time + fi);\n\n        // 计算当前像素与雪花位置的距离\n        float dist = distance(uv * screenSize, snowPos);\n\n        // 如果像素位于雪花内，则设置雪花颜色\n        if (dist < flakeSize * screenSize.x) {\n            float alpha = smoothstep(flakeSize * screenSize.x, 0.0, dist);\n            snowColor = mix(snowColor, vec4(1.0, 1.0, 1.0, alpha), alpha);\n            cover = true;\n        }\n    }\n\n    if (cover == true) {\n        // 将雪花叠加到背景图片上\n        gl_FragColor = mix(bgColor, snowColor, snowColor.a);\n        gl_FragColor.a = 1.0;\n    } else {\n        gl_FragColor = bgColor;\n        gl_FragColor.a = 1.0;\n    }\n}\n",
  "sobel": "#version 100\nprecision mediump float;\n\nvarying vec2 uv; // 材质 UV\nuniform sampler2D screenTexture; // 屏幕材质\nuniform vec2 screenSize; // 屏幕大小\nuniform float strength; // %1.0%\nuniform float time; // 时间，以秒为单位\n\nvec4 getcolor(vec2 uv)\n{\n    return texture2D(screenTexture, uv);\n}\n\nvoid main()\n{\n    // Sobel 核\n    float kernelX[9];\n    float kernelY[9];\n\n    kernelX[0] = -1.0; kernelX[1] = 0.0; kernelX[2] = 1.0;\n    kernelX[3] = -2.0; kernelX[4] = 0.0; kernelX[5] = 2.0;\n    kernelX[6] = -1.0; kernelX[7] = 0.0; kernelX[8] = 1.0;\n\n    kernelY[0] = -1.0; kernelY[1] = -2.0; kernelY[2] = -1.0;\n    kernelY[3] = 0.0;  kernelY[4] = 0.0;  kernelY[5] = 0.0;\n    kernelY[6] = 1.0;  kernelY[7] = 2.0;  kernelY[8] = 1.0;\n\n    vec2 texOffset[9];\n    texOffset[0] = vec2(-1.0, -1.0) / screenSize;\n    texOffset[1] = vec2( 0.0, -1.0) / screenSize;\n    texOffset[2] = vec2( 1.0, -1.0) / screenSize;\n    texOffset[3] = vec2(-1.0,  0.0) / screenSize;\n    texOffset[4] = vec2( 0.0,  0.0) / screenSize;\n    texOffset[5] = vec2( 1.0,  0.0) / screenSize;\n    texOffset[6] = vec2(-1.0,  1.0) / screenSize;\n    texOffset[7] = vec2( 0.0,  1.0) / screenSize;\n    texOffset[8] = vec2( 1.0,  1.0) / screenSize;\n\n    float sumX = 0.0;\n    float sumY = 0.0;\n\n    vec3 originalColor = texture2D(screenTexture, uv).rgb;\n\n    for (int i = 0; i < 9; i++) {\n        vec4 color = getcolor(uv + texOffset[i]);\n        float intensity = dot(color.rgb, vec3(0.299, 0.587, 0.114));  // 转为灰度\n        sumX += intensity * kernelX[i];\n        sumY += intensity * kernelY[i];\n    }\n\n    float sobelValue = sqrt(sumX * sumX + sumY * sumY);\n\n    vec3 finalColor = mix(originalColor, vec3(sobelValue), strength);\n\n    gl_FragColor = vec4(finalColor, 1.0);\n}\n",
  "two_tone": "#version 100\nprecision mediump float;\n\nvarying vec2 uv;\nuniform sampler2D screenTexture;\n\nuniform vec4 color1; // %0.0, 0.0, 0.0, 1.0%\nuniform vec4 color2; // %0.6, 0.6, 1.0, 1.0%\nuniform float threshold; // %0.5%\n\nvoid main() {\n    vec4 texColor = texture2D(screenTexture, uv);  // 获取纹理颜色\n    float brightness = dot(texColor.rgb, vec3(0.299, 0.587, 0.114));  // 计算亮度\n\n    vec4 finalColor = brightness < threshold ? color1 : color2;  // 根据亮度应用不同颜色\n    // 使用纹理的透明度来调整最终的 alpha\n    gl_FragColor = vec4(finalColor.rgb, 1.0);\n}\n",
  "underwater": "#version 100\nprecision mediump float;\n\nvarying vec2 uv; // 纹理坐标\nuniform float time;  // 时间变量，用于波浪效果\nuniform vec2 screenSize;  // 屏幕分辨率\nuniform sampler2D screenTexture; // 输入的屏幕纹理\n\nvoid main() {\n    vec2 uvCoord = uv;\n    vec4 color = texture2D(screenTexture, uvCoord);\n    \n    // 计算波浪效果\n    float wave = sin(uvCoord.y * 10.0 + time * 5.0) * 0.01;\n    uvCoord.x += wave;\n\n    // 为水下效果添加蓝绿色调\n    vec3 underwaterColor = vec3(0.0, 0.4, 0.6);\n    color.rgb = mix(color.rgb, underwaterColor, 0.4);\n\n    // 添加一些模糊来模拟水的光线散射\n    float blur = sin(time * 1.5) * 0.005;\n    uvCoord += vec2(blur, blur);\n    \n    vec4 blurColor = texture2D(screenTexture, uvCoord) * 0.9;\n    color = mix(color, blurColor, 0.5);\n\n    // 输出最终颜色\n    gl_FragColor = color;\n    gl_FragColor.a = 1.0;\n}\n",
  "wave": "#version 100\nprecision mediump float;\n\nvarying vec2 uv;\nuniform sampler2D screenTexture;\n\nuniform float time;\nuniform float speed; // %5.0%\nuniform vec2 direction; // %1.0, 0.0%\nuniform float frequency; // %30.0%\nuniform float amplitude; // %0.008%\n\nconst vec2 rectMin = vec2(0.0, 0.0);\nconst vec2 rectMax = vec2(1.0, 1.0);\nvec4 getcolor(vec2 uv)\n{\n\tif (uv.x >= rectMin.x && uv.x <= rectMax.x && uv.y >= rectMin.y && uv.y <= rectMax.y)\n\t\treturn texture2D(screenTexture, uv);\n\telse\n\t\treturn vec4(0, 0, 0, 1);\n}\n\nvoid main() {\n\n    // 计算方向向量影响下的波动\n    float wave = sin(dot(uv, direction * frequency) + time * speed) * amplitude;\n\n    // 将波动效果应用到纹理坐标上，使其沿给定方向和速度波动\n    vec2 distortedTexCoord = vec2(uv.x + wave, uv.y + wave);\n\n    gl_FragColor = getcolor(distortedTexCoord);\n\tgl_FragColor.a = 1.0;\n}\n",
  "chromatic": "#version 100\nprecision mediump float;\nvarying vec2 uv;\nuniform sampler2D screenTexture;\nuniform float sampleCount; // %3% int 1..64\nuniform float power; // %0.01%\nvoid main() {\n  vec3 sum = vec3(0.0);\n  vec3 c = vec3(0.0);\n  vec2 offset = (uv - vec2(0.5)) * vec2(1.0, -1.0);\n  int n = int(sampleCount);\n  if (n < 1) n = 1;\n  if (n > 64) n = 64;\n  float den = float(n > 1 ? n - 1 : 1);\n  for (int i = 0; i < 64; ++i) {\n    if (i >= n) break;\n    float t = 2.0 * float(i) / den;\n    vec3 slice = vec3(1.0 - t, 1.0 - abs(t - 1.0), t - 1.0);\n    slice = max(slice, vec3(0.0));\n    sum += slice;\n    vec2 slice_offset = (t - 1.0) * power * offset;\n    c += slice * texture2D(screenTexture, uv + slice_offset).rgb;\n  }\n  gl_FragColor = vec4(c / max(sum, vec3(0.0001)), 1.0);\n}",
  "circleBlur": "#version 100\nprecision mediump float;\nuniform float uScale;\nvarying vec2 uv;\nuniform vec2 screenSize;\nuniform sampler2D screenTexture;\nuniform float size; // %10.0%\nvoid main() {\n  vec4 c = texture2D(screenTexture, uv);\n  float len = dot(c, c);\n  vec2 pixel_size = 1.0 / screenSize;\n  int n = int(size);\n  if (n < 1) n = 1;\n  n = int(float(n) * max(uScale, 0.25)); if (n < 1) n = 1; if (n > 16) n = 16;\n  int span = n * 2 + 1;\n  for (int xi = 0; xi < 64; ++xi) {\n    if (xi >= span) break;\n    float x = float(xi) - float(n);\n    for (int yi = 0; yi < 64; ++yi) {\n      if (yi >= span) break;\n      float y = float(yi) - float(n);\n      if (x * x + y * y > float(n) * float(n)) continue;\n      vec4 new_c = texture2D(screenTexture, uv + pixel_size * vec2(x, y));\n      float new_len = dot(new_c, new_c);\n      if (new_len > len) { len = new_len; c = new_c; }\n    }\n  }\n  gl_FragColor = vec4(c.rgb, 1.0);\n}",
  "fisheye": "#version 100\nprecision mediump float;\nvarying vec2 uv;\nuniform vec2 screenSize;\nuniform sampler2D screenTexture;\nuniform float power; // %-0.1%\nvoid main() {\n  vec2 p = vec2(uv.x, uv.y * screenSize.y / screenSize.x);\n  float aspect = screenSize.x / screenSize.y;\n  vec2 m = vec2(0.5, 0.5 / aspect);\n  vec2 d = p - m;\n  float r = sqrt(dot(d, d));\n  float new_power = (2.0 * 3.141592 / (2.0 * sqrt(dot(m, m)))) * power;\n  float bind = new_power > 0.0 ? sqrt(dot(m, m)) : (aspect < 1.0 ? m.x : m.y);\n  vec2 nuv;\n  vec2 dir = r > 0.0001 ? normalize(d) : vec2(0.0, 0.0);\n  if (new_power > 0.0)\n    nuv = m + dir * tan(r * new_power) * bind / tan(bind * new_power);\n  else\n    nuv = m + dir * atan(r * -new_power * 10.0) * bind / atan(-new_power * bind * 10.0);\n  gl_FragColor = vec4(texture2D(screenTexture, vec2(nuv.x, nuv.y * aspect)).rgb, 1.0);\n}",
  "glitch": "#version 100\nprecision highp float;\nvarying vec2 uv;\nuniform sampler2D screenTexture;\nuniform float time;\nuniform float power; // %0.03%\nuniform float rate; // %0.6% 0..1\nuniform float speed; // %5.0%\nuniform float blockCount; // %30.5%\nuniform float colorRate; // %0.01% 0..1\nfloat my_trunc(float x) { return x < 0.0 ? -floor(-x) : floor(x); }\nfloat random(float seed) {\n  return fract(543.2543 * sin(dot(vec2(seed, seed), vec2(3525.46, -54.3415))));\n}\nvoid main() {\n  float enable_shift = float(random(my_trunc(time * speed)) < rate);\n  vec2 fixed_uv = uv;\n  fixed_uv.x += (random((my_trunc(uv.y * blockCount) / blockCount) + time) - 0.5) * power * enable_shift;\n  vec4 pixel_color = texture2D(screenTexture, fixed_uv);\n  pixel_color.r = mix(pixel_color.r, texture2D(screenTexture, fixed_uv + vec2(colorRate, 0.0)).r, enable_shift);\n  pixel_color.b = mix(pixel_color.b, texture2D(screenTexture, fixed_uv + vec2(-colorRate, 0.0)).b, enable_shift);\n  gl_FragColor = vec4(pixel_color.rgb, 1.0);\n}",
  "grayscale": "#version 100\nprecision mediump float;\nvarying vec2 uv;\nuniform sampler2D screenTexture;\nuniform float factor; // %1.0% 0..1\nvoid main() {\n  vec3 color = texture2D(screenTexture, uv).xyz;\n  vec3 lum = vec3(0.299, 0.587, 0.114);\n  vec3 gray = vec3(dot(lum, color));\n  gl_FragColor = vec4(mix(color, gray, factor), 1.0);\n}",
  "noise": "#version 100\nprecision highp float;\nvarying vec2 uv;\nuniform sampler2D screenTexture;\nuniform float seed; // %81.0%\nuniform float power; // %0.03% 0..1\nvec2 random(vec2 pos) {\n  return fract(sin(vec2(dot(pos, vec2(12.9898, 78.233)), dot(pos, vec2(-148.998, -65.233)))) * 43758.5453);\n}\nvoid main() {\n  vec2 new_uv = uv + (random(uv + vec2(seed, 0.0)) - vec2(0.5, 0.5)) * power;\n  gl_FragColor = vec4(texture2D(screenTexture, new_uv).rgb, 1.0);\n}",
  "pixel": "#version 100\nprecision mediump float;\nvarying vec2 uv;\nuniform vec2 screenSize;\nuniform sampler2D screenTexture;\nuniform float size; // %10.0%\nvoid main() {\n  vec2 factor = screenSize / max(size, 1.0);\n  float x = floor(uv.x * factor.x + 0.5) / factor.x;\n  float y = floor(uv.y * factor.y + 0.5) / factor.y;\n  gl_FragColor = vec4(texture2D(screenTexture, vec2(x, y)).rgb, 1.0);\n}",
  "radialBlur": "#version 100\nprecision mediump float;\nvarying vec2 uv;\nuniform sampler2D screenTexture;\nuniform float centerX; // %0.5% 0..1\nuniform float centerY; // %0.5% 0..1\nuniform float power; // %0.01% 0..1\nuniform float sampleCount; // %6% int 1..64\nvoid main() {\n  vec2 direction = uv - vec2(centerX, centerY);\n  vec3 c = vec3(0.0);\n  int n = int(sampleCount);\n  if (n < 1) n = 1;\n  if (n > 64) n = 64;\n  float f = 1.0 / float(n);\n  for (int i = 0; i < 64; ++i) {\n    if (i >= n) break;\n    c += texture2D(screenTexture, uv - power * direction * float(i)).rgb * f;\n  }\n  gl_FragColor = vec4(c, 1.0);\n}",
  "shockwave": "#version 100\nprecision mediump float;\nvarying vec2 uv;\nuniform vec2 screenSize;\nuniform sampler2D screenTexture;\nuniform float progress; // %0.2% 0..1\nuniform float centerX; // %0.5% 0..1\nuniform float centerY; // %0.5% 0..1\nuniform float width; // %0.1%\nuniform float distortion; // %0.8%\nuniform float expand; // %10.0%\nvoid main() {\n  float aspect = screenSize.y / screenSize.x;\n  vec2 center = vec2(centerX, (centerY - 0.5) * aspect + 0.5);\n  vec2 tex_coord = vec2(uv.x, (uv.y - 0.5) * aspect + 0.5);\n  float dist = distance(tex_coord, center);\n  if (progress - width <= dist && dist <= progress + width) {\n    float diff = dist - progress;\n    float scale_diff = 1.0 - pow(abs(diff * expand), distortion);\n    float dt = diff * scale_diff;\n    vec2 dir = dist > 0.0001 ? normalize(tex_coord - center) : vec2(0.0);\n    tex_coord += (dir * dt) / max(progress * dist * 40.0, 0.0001);\n    vec2 uv2 = vec2(tex_coord.x, (tex_coord.y - 0.5) / aspect + 0.5);\n    gl_FragColor = texture2D(screenTexture, uv2);\n    gl_FragColor += (gl_FragColor * scale_diff) / max(progress * dist * 40.0, 0.0001);\n  } else {\n    gl_FragColor = texture2D(screenTexture, uv);\n  }\n  gl_FragColor.a = 1.0;\n}",
  "vignette": "#version 100\nprecision mediump float;\nvarying vec2 uv;\nuniform vec2 screenSize;\nuniform sampler2D screenTexture;\nuniform vec4 color; // %0.0, 0.0, 0.0, 1.0%\nuniform float extend; // %0.25% 0..1\nuniform float radius; // %15.0%\nvoid main() {\n  vec2 new_uv = uv * (1.0 - uv.yx);\n  float vig = new_uv.x * new_uv.y * radius;\n  vig = pow(max(vig, 0.0), extend);\n  gl_FragColor = mix(color, texture2D(screenTexture, uv), vig);\n  gl_FragColor.a = 1.0;\n}",
  "__diag": "#version 100\nprecision highp float;\nvarying vec2 uv;\nuniform sampler2D screenTexture;\nuniform vec2 screenSize;\nvoid main() {\n  if (gl_FragCoord.y > screenSize.y * 0.5) { gl_FragColor = vec4(1.0, 0.0, 0.0, 1.0); }\n  else { gl_FragColor = vec4(0.0, 0.0, 1.0, 1.0); }\n}",
};
